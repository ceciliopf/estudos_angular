{
  "name": "estudos_angular",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "server": "cd algamoney && mvnw.cmd spring-boot:run",
    "client": "npm start --prefix algamoney-ui",
    "start": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue,green\" \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
